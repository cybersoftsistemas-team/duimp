name: Build

on:
  pull_request:
    branches:
      - develop
      - main
    types: [opened, synchronize, reopened, closed]

jobs:
  validate-main-pr:
    name: "🚨 Validação de PR develop → main"
    runs-on: self-hosted
    steps:
      - name: Check pull request source branch
        if: ${{ github.base_ref == 'main' && github.head_ref != 'develop' }}
        run: |
          echo "❌ Pull request inválido: somente pull request de 'develop' para 'main' é permitido!"
          exit 1

  build-homologation:
    name: Build Homologação
    if: ${{ github.base_ref == 'develop' && github.event.pull_request.merged == true && github.event.action == 'closed' }}
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Compile project
        run: echo "Publicando build em ambiente de homologação"

  build-production:
    name: Build Produção
    if: ${{ github.base_ref == 'main' && github.head_ref == 'develop' && github.event.pull_request.merged == true }}
    runs-on: self-hosted
    needs: validate-main-pr
    steps:
      - uses: actions/checkout@v4
      - name: Compile project
        run: echo "Publicando build em ambiente de produção"
