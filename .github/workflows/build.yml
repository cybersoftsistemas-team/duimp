name: Build

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      
      - name: compile-project 
        run: echo "Publicando build em ambiente de produção"
