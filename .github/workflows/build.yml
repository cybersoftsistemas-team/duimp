name: Build

on:
  pull_request:
    branches:
      - develop
      - main

jobs:
  build-develop:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      
      - name: Build Homologação
        run: echo "Publicando build em ambiente de homologação"
